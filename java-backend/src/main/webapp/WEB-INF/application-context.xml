<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans  http://www.springframework.org/schema/beans/spring-beans.xsd
                http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Поиск бинов -->
    <context:component-scan base-package="by.dream.team.shop"/>

    <!-- Бин для добавления файлов .properties в окружение -->
    <bean class="by.dream.team.shop.spring.PropertySourcesConfigurer" init-method="init">
        <property name="propertySources">
            <list>
                <bean class="org.springframework.core.io.support.ResourcePropertySource" scope="prototype">
                    <constructor-arg value="classpath:database.properties"/>
                </bean>
            </list>
        </property>
    </bean>

    <!-- Бин для подключения к БД -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate">
        <constructor-arg type="javax.sql.DataSource" ref="dataSource"/>
    </bean>

    <!-- Настройки подключения к БД -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="com.mysql.jdbc.Driver" />
        <property name="url" value="jdbc:mysql://${database.host}/${database.name}?useUnicode=true&amp;characterEncoding=utf8" />
        <property name="username" value="${database.user}" />
        <property name="password" value="${database.password}" />
    </bean>
</beans>